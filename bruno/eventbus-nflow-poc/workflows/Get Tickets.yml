info:
  name: Get Tickets
  type: http
  seq: 1

http:
  method: GET
  url: "{{eventbus_nflow_url}}/api/workflows/tickets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
